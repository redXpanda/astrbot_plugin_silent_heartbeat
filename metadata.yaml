name: astrbot_plugin_silent_heartbeat
display_name: Silent Heartbeat
short_desc: A memory-aware heartbeat that defaults to silence.
desc: |
  Reviews one authorized private memory domain and explicit group domains.
  It stays silent unless a validated model decision permits an action.
version: 0.2.0
author: local
repo: https://github.com/redXpanda/astrbot_plugin_silent_heartbeat
astrbot_version: ">=4.16,<5"
tags:
  - heartbeat
  - automation
  - proactive
